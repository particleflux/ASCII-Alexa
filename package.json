{
  "name": "ASCII-Alexa",
  "version": "1.0.0",
  "description": "Alexa ASCII skill",
  "main": "index.js",
  "scripts": {
    "test": "nyc mocha",
    "test:CI": "codeclimate-test-reporter before-build && nyc mocha && nyc report --reporter=lcov && codeclimate-test-reporter after-build --exit-code $?",
    "clean": "rm -r build/ .lambda/",
    "invoke": "node-lambda run"
  },
  "keywords": [
    "alexa",
    "skill",
    "ascii"
  ],
  "author": "Stefan Linke",
  "license": "UNLICENSED",
  "dependencies": {
    "alexa-sdk": "^1.0.6"
  },
  "devDependencies": {
    "alexa-skill-test-framework": "^1.1.0",
    "mocha": "^3.4.2",
    "node-lambda": "^0.11.3",
    "nyc": "^11.0.3"
  }
}
